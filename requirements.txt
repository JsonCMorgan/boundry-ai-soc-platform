# Pinned for reproducible installs (bump intentionally when upgrading).
Flask==3.1.0
Werkzeug==3.1.6
Jinja2==3.1.6
MarkupSafe==3.0.3
click==8.3.1
itsdangerous==2.2.0
blinker==1.9.0

# Auth
bcrypt==5.0.0

# Database
psycopg2-binary==2.9.10

# Rate limiting
Flask-Limiter==4.1.1
limits==5.8.0
packaging==26.0
typing_extensions==4.15.0
typing-inspection==0.4.2

# CSRF protection
Flask-WTF

# Report rendering
Markdown==3.10.2

# YAML playbook loading
PyYAML==6.0.2

# AI report generation
anthropic==0.49.0

# RSS feed parsing (breach intel ticker)
feedparser==6.0.11

# IP reputation lookups (AbuseIPDB)
requests==2.33.1

# Transactional email (alerts + weekly digest)
resend==2.30.1

# 2FA — TOTP (Google Authenticator compatible)
pyotp==2.9.0
qrcode[pil]==8.1

# Production server
gunicorn==26.0.0
